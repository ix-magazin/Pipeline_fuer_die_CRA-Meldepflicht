{
  "name": "@beispielshop/api",
  "version": "1.0.0",
  "private": true,
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --noEmit && esbuild src/main.ts --bundle --packages=external --platform=node --target=node22 --outfile=dist/main.js",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "@beispielshop/schema-tools": "workspace:*",
    "@nestjs/common": "^11.0.0",
    "@nestjs/core": "^11.0.0",
    "@nestjs/platform-express": "^11.0.0",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.5.0"
  }
}
