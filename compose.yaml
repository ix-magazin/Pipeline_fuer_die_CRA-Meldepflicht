services:
  api:
    build: apps/api
    ports:
      - "3001:3001"
  web:
    build: apps/web
    depends_on:
      - api
    environment:
      API_URL: http://api:3001
    ports:
      - "3000:3000"
